"use strict";
document.addEventListener('DOMContentLoaded', () => {
    const main = document.querySelector('main');
    main === null || main === void 0 ? void 0 : main.classList.add('opacity-100');
});
